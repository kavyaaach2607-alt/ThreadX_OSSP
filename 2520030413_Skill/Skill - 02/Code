include<stdio.h>
int main() {
char buffer[100];
char ch;
int i = 0;
printf("Enter command: ");
while(1) {
ch = getchar();
if(ch == '\n') {
break;
}
if(ch == '\b' && i > 0) {
i--;
}
else {
buffer[i++] = ch;
}
}
buffer[i] = '\0';
printf("You entered: %s\n", buffer);
return 0;
}
