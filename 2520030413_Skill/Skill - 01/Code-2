include<stdio.h>
#include<unistd.h>
#include <sys/wait.h>
int main() {
int pid = fork();
if(pid == 0) {
printf("Child Proces\n");
printf("Child PID = %d\n", getpid());
printf("Parent PID = %d\n", getppid());
}
else {
printf("Parent Proces\n");
printf("Parent PID = %d\n", getpid());
printf("Child PID = %d\n", pid);
wait(NULL);
}
return 0;
}
